name: endophynd-annotate
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - itsx>=1.1
  - hmmer>=3.4
  - vsearch>=2.28
  - blast>=2.14       # assign_locus_blast.py (makeblastdb + blastn)
  - pyyaml>=6.0       # parse_itsx.py, gate.py, assign_locus_blast.py
